# Gym Management Backend

This folder contains the Express backend for the Gym Manager App.

## Quickstart

1. Copy `.env.example` to `.env` and fill in your database credentials.
2. Install dependencies:
   ```bash
   npm install
