// Later, you’ll define your DB schema here (e.g., with Sequelize or Knex).
// For now, it’s empty so Git tracks the folder.

export const placeholderModel = {};
// This is a placeholder model to verify folder wiring